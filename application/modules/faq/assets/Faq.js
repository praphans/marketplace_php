/*$(function(){
	var isActive = false;
    var current = decodeURIComponent(location.pathname);
    $('.ts_nav li a').each(function(){
        var $this = $(this);
		
        if(!isActive && $this.attr('href').indexOf(current) !== -1){
			console.log($this.attr('href')+" : "+current);
            $this.parent().addClass('active');
			isActive = true;
        }
    })
})*/